# 服务端口
PORT=3001

# 访问 B 站接口所需登录态 cookie（从浏览器复制，至少包含 SESSDATA / bili_jct / buvid3）。
# 不配也能抓部分公开数据，但弹幕/评论完整度和风控通过率会下降。
BILI_COOKIE=

# LLM 网关（OpenAI 兼容的 /chat/completions）。留空时分析接口回退到本地启发式摘要，
# 保证框架能端到端跑通 demo。
LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL=
